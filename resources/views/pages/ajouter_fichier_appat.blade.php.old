@extends('template')

@section('content')

<!-- Formulaire ajouter fichier appat-->

<div class="card p-4">
  <p> we have to put two button in this form 
    first is for chosing path and the econd is for 
    chosing the file
  </p>
  @if ($errors->any())
    <div class="alert alert-danger">
        <ul>
            @foreach ($errors->all() as $error)
                <li>{{ $error }}</li>
            @endforeach
        </ul>
    </div>
  @endif
  <h4 class="display-8">Upload a File</h4>
  <hr class="my-4">
  <form action="/fichier-appat" method="post" enctype="multipart/form-data">
    @csrf

    <div class="mb-3">
      <label for="formFile" class="form-label">Parcourir le chemin de fichier</label>
      <input class="form-control" type="file" id="formFile" name="fichier">
      <a class="form-control" href="select_file">Select file</a>
    </div>

    <div class="mb-3">
      <label for="formFile" class="form-label">Choisir le fichier appat</label>
      <input class="form-control" type="file" id="formFile" name="fichier">
    </div>
    <input type="submit" value="Ajouter" class="btn btn-primary">
  </form>
</div>
<!-- Fin de Formulaire d'ajouter un nouveau fichier-->



@endsection

